# Rule

Horizontal or vertical line separator.

**Constructors:**
- `NewHRule(class, style string) *Rule`
- `NewVRule(class, style string) *Rule`

`style` is a theme border name (e.g. `"single"`, `"double"`).
