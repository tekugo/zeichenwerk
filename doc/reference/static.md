# Static

Simple text display widget.

**Constructor:** `NewStatic(id, class, text string) *Static`

## Methods

- `SetAlignment(align string)` — sets text alignment: `"left"`, `"center"`, `"right"`
- `SetText(text string)` — updates displayed text
