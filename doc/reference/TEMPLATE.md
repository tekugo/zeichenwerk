# WidgetName

One-sentence description of what the widget does.

**Constructor:** `NewWidgetName(id, class string, ...) *WidgetName`

Brief note on any non-obvious constructor parameters, if needed.

## Methods

- `Method(params) returnType` — what it does

## Events

| Event | Data | Description |
|-------|------|-------------|
| `"eventname"` | `type` | When it fires |

## Notes

Flags: `"flagname"`, ...

Any other notable behaviour, keyboard shortcuts, or constraints.
