# Digits

Large-format display using ASCII art characters.

**Constructor:** `NewDigits(id, class, text string) *Digits`

## Methods

- `SetText(text string)` — updates displayed text

## Notes

Supported characters: `0–9`, `A–F`, `:`, `.`, `-`
